<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kmust.Karelu.mapper.TousuMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.kmust.Karelu.entity.Tousu">
        <id column="tousuid" property="tousuid" />
        <result column="containthing" property="containthing" />
        <result column="uid" property="uid" />
        <result column="createdate" property="createdate" />
        <result column="bedone" property="bedone" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        tousuid, containthing, uid, createdate, bedone
    </sql>

</mapper>
